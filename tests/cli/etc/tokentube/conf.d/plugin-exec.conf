events {
	event USER_CREATED {
		exec = "/usr/bin/logger"
		detach = true
	}
	event USER_UPDATED {
		exec = "/usr/bin/logger"
		detach = true
	}
	event USER_DELETED {
		exec = "/usr/bin/logger"
		detach = true
	}
}

