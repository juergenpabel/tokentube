events {
	event USER-CREATED {
		exec = "/usr/bin/logger"
		detach = true
	}
	event USER-UPDATED {
		exec = "/usr/bin/logger"
		detach = true
	}
	event USER-DELETED {
		exec = "/usr/bin/logger"
		detach = true
	}
}

